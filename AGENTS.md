# プロジェクト行動規範

## 言語

思考・回答・ドキュメント・コミットメッセージはすべて日本語で記述する。
コード内の識別子は既存の慣習に従う。

## 完了報告の条件（最優先）

以下の証拠を提示していない状態で「完了」「動作します」「修正しました」
「問題ありません」と報告することを禁止する。

1. ビルド／実行コマンドの実際の出力
2. テストがある場合はその実行結果
3. UI 変更を伴う場合は、ブラウザで対象画面を開いたスクリーンショット

証拠を提示できない場合は「未検証」と明記し、理由を述べること。
推測で成否を述べてはならない。

## 自己検証

- 実装後、必ず自分で実行して結果を観測すること。
- エラーが出た場合、エラーメッセージ全文を提示してから修正に入ること。
- 「おそらく動くはず」「〜だと思われます」という表現で検証を代替しない。

## 報告の構造

- 冒頭に結論を 1〜3 行で述べる。
- 「確認済みの事実」と「未確認・推測」を明確に分離する。
- 不確実な箇所には必ずその旨を明示する。

## 禁止する振る舞い

- 指示されていないファイルの削除・大規模な書き換え
- 既存の動作を壊す変更を、その旨を告げずに行うこと
- ユーザーへの迎合。設計上のリスクや論理的破綻は率直に指摘すること
